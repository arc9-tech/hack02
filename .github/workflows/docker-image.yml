name: Docker Image CI

on:
  push:
    branches: [ "main" , "dev", "devops"]
  # pull_request:
  #   branches: [ "main" ]

jobs:
  build:
    runs-on: self-hosted

    steps:
    - uses: actions/checkout@v3
    - uses: actions/setup-java@v3
      with:
        distribution: 'temurin'
        java-version: '8'
        cache: 'maven'
    - name: Build with Maven
      run: mvn -B clean package --file pom.xml
    # - name: Build the Docker image for gateway service
    #   working-directory: ./gateway
    #   run: docker build . --file Dockerfile --tag gateway-service:"$TAG"

    - name: Login to JFrog 🐸
      uses: docker/login-action@v1
      with:
        registry: babeswithbothacks.jfrog.io
        username: ${{ secrets.JFROG_USER_WRITER }}
        password: ${{ secrets.JFROG_PASSWORD_WRITER }}

    - name: Build and push media service🐳
      if: always()
      id: docker_build_media
      uses: docker/build-push-action@v2
      with:
        context: ./media-service
        push: ${{ github.event_name != 'pull_request' }}
        tags: babeswithbothacks.jfrog.io/docker/media-service:0.0.1

    - name: Build and push user service🐳
      if: always()
      id: docker_build_user
      uses: docker/build-push-action@v2
      with:
        context: ./user
        push: ${{ github.event_name != 'pull_request' }}
        tags: babeswithbothacks.jfrog.io/docker/user-service:0.0.1

    - name: Build and push gateway service 🐳
      if: always()
      id: docker_build_gateway
      uses: docker/build-push-action@v2
      with:
        context: ./gateway
        push: ${{ github.event_name != 'pull_request' }}
        tags: babeswithbothacks.jfrog.io/docker/gateway-service:0.0.1
